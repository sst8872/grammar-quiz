{
  "scripts": {
    "start": "parcel index.html no-source-maps",
    "build": "parcel build index.html no-source-maps -d dist --public-url ./"
  },
  "devDependencies": {
    "cssnano": "^4.1.10"
  }
}
